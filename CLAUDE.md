# [Project Name]

> **How this file is filled:** Run `/onboard` (existing projects) or `/setup` (greenfield) first.
> Each section is labeled **[discovered]** (auto-filled from a scan — edit if wrong) or **[declared]** (your answer to a question — change anytime).
> Sections marked `(inferred)` are educated guesses; verify before relying on them.

## Project Overview

**[declared]** Two or three sentences. What this project does, who uses it, the stack at a glance. `/onboard` will draft this from your README and package manifest; you confirm or rewrite.

## Quick Start

**[discovered]** Commands the project actually exposes. `/onboard` reads these from package.json scripts, Makefile, justfile, Taskfile, or the equivalent.

```bash
# install:    <command>
# run / dev:  <command>
# test:       <command>
# lint:       <command>
# typecheck:  <command>
# build:      <command>
```

If any of the above is missing, leave the line as `# <name>: (none configured)` — other skills will skip the step rather than guess.

## Architecture

**[discovered]** Real files and their roles. `/onboard` fills this from entry points, largest modules, and CODEOWNERS if present.

| Path | Role |
|------|------|
| `src/...` | (filled by /onboard) |

### Service / URL map (if applicable)

If the project runs multiple services, list them. `/look` and `/check` read this:

| Service | URL | Started by |
|---------|-----|------------|
| frontend | http://localhost:5173 | `pnpm dev` |
| api | http://localhost:3000 | `pnpm api` |

## Existing Tooling

**[discovered]** What's already configured in the repo. Other skills call these instead of guessing.

| Tool | Command | Notes |
|------|---------|-------|
| linter | (e.g., `pnpm lint`) | |
| formatter | (e.g., `pnpm format`) | |
| type-checker | (e.g., `pnpm typecheck`) | |
| pre-commit | (e.g., husky / lefthook / pre-commit) | |
| CI | (e.g., GitHub Actions) | |
| reference lookup | (e.g., `tsc --noEmit`, `go list`, `ripgrep`) | Used by `/guard` |

## Don't Touch

**[discovered]** Paths Claude should not edit without explicit instruction. `/onboard` proposes; you confirm.

- `node_modules/`, `vendor/`, `dist/`, `build/` (auto)
- Generated files (auto-detected — `// GENERATED`, `# autogenerated`)
- Migrations older than 30 days (changing them is almost always a bug)
- Vendored third-party code under `third_party/`, `external/`, `lib/vendor/`
- (add your own — e.g., legacy modules pending rewrite)

## Risk Surfaces

**[discovered + declared]** Sensitive areas where extra caution applies. `/diff`, `/guard`, and the agents elevate severity when these are touched.

- (e.g., `src/auth/` — session, login, JWT)
- (e.g., `src/payments/` — Stripe, billing)
- (e.g., `migrations/` — schema changes)
- (e.g., `.env*` — secrets)

## Accepted Risks

**[declared]** Patterns that *look* dangerous but the team has accepted. Stops the security-reviewer agent from re-flagging them every run.

- (e.g., `eval` used in `src/sandbox/repl.ts` — sandboxed, reviewed 2024-10)
- (e.g., `innerHTML` in `src/editor/markdown.ts` — input is DOMPurify'd one level up)

## Priorities (in order)

**[declared]** When tradeoffs collide, follow this order. `/onboard` proposes defaults from your risk-tolerance answer.

1. **Correctness** — (what correctness means here: data integrity? exact arithmetic? deterministic output?)
2. **Compatibility** — (which APIs / data shapes / configs must not break?)
3. **Performance** — (any hot paths or latency budgets?)
4. **User experience** — (what users notice and care about)
5. **Code elegance** — never at the cost of the above

## Quality Bar

**[declared]** A change is not done until:

- Tests pass (`<test command>`)
- Lint clean (`<lint command>`)
- Type-check clean (`<typecheck command>` — skip if not configured)
- No debug artifacts left behind
- (add: PR template completed, CHANGELOG updated, etc. — if applicable)

## Anti-Patterns (WRONG -> RIGHT)

**[discovered + declared]** Real patterns from this codebase's bug history. `/onboard` mines `git log` for fix/revert/hotfix commits and proposes candidates; you confirm before they land here. Empty is fine — better than fictional examples.

| WRONG | RIGHT | Why | Evidence |
|-------|-------|-----|----------|
| (e.g., direct `Date.now()` in tests) | (use injected clock) | flaky test failures | 4 reverts in 2025 |

## Key Conventions

**[discovered]** Patterns observed in the codebase. `/onboard` fills these from sampling the largest files.

### Code Style

- **Language(s):** (filled by /onboard)
- **Naming:** (camelCase / snake_case / kebab-case observed in functions, files, constants)
- **Imports:** (relative / absolute / barrel files / named vs default)
- **Error handling:** (throw / Result / error-as-value)
- **Async style:** (async-await / promises / callbacks)
- **Logger:** (project logger import or `console`)

### Critical Rules

**[declared]** The things that must never go wrong. `/onboard` seeds these from your risk-tolerance answer + detected risk surfaces.

1. (e.g., Never commit secrets — pre-commit hook configured, but human review still primary)
2. (e.g., Never push to default branch directly — open a PR)
3. (e.g., Never run schema migrations from a feature branch in isolation)

## Commit & Workflow Policy

**[declared]** How changes flow from working tree to main.

- **Commit policy:** (auto-commit | stage-only | draft-PR | never-commit) — set during `/onboard`, used by `/wrap`
- **Default branch:** (auto-detected — `main` / `master` / `develop` / etc.)
- **Branch protection:** (block direct pushes | warn-and-confirm | no protection)
- **Commit message convention:** (auto-detected — e.g., `[PROJ-123] subject`, Conventional Commits, or prose)
- **PR template:** (path if `.github/PULL_REQUEST_TEMPLATE.md` exists — `/wrap` will surface required sections)
- **CHANGELOG:** (update on every change | only on release | not used)

## Output Preferences

**[declared]** How Claude should communicate in this project.

- Keep responses short — diffs speak for themselves
- Don't summarize what you just did at the end of every response
- After config changes: proactively surface risks
- (Add: required vocabulary, terms to avoid, preferred level of explanation)

## Automation

- Skills in `.claude/skills/` — `/onboard`, `/setup`, `/plan`, `/wrap`, `/check`, `/guard`, `/diff`, `/undo`, `/look`, `/save-conversation`
- Agents in `.claude/agents/` — code-reviewer, error-trap-monitor, security-reviewer
- Hooks in `.claude/settings.json` — auto-fire on edits, commits, and session end
- Plans in `.claude/plans/` — persistent plan documents from `/plan` Tier 2-3
- Onboarding report in `.claude/onboarding.md` — what `/onboard` found, what it confirmed, what it deferred

## Data Sources

**[declared]** APIs, databases, queues, external services. `/onboard` will spot integration directories and propose entries.

- (e.g., Postgres — connection pool in `src/db/`, migrations in `migrations/`)
- (e.g., Stripe API — webhook handler in `src/webhooks/stripe.ts`)

## Error Handling

**[discovered + declared]** How errors should propagate in this project. `/onboard` reads existing patterns; you declare the policy.

- Strategy: (throw + catch at boundary | Result types | sentinel returns)
- Logging: (logger name and level convention)
- User-facing: (toast / inline message / silent retry — depends on the surface)

## Security Notes

**[declared]** Secrets management, auth patterns, what should never be committed. Seeded from detected risk surfaces and your risk-tolerance answer.

- Secrets handled via: (env vars / vault / KMS — describe)
- Never commit: (list — e.g., `.env`, `*.pem`, `secrets/`)
- Auth model: (session / JWT / OAuth — and where the boundary is enforced)

## Module Ownership

**[discovered]** From `CODEOWNERS` if present. Helps `/wrap` surface review-routing notes and `/guard` flag cross-team boundaries.

| Path | Owner |
|------|-------|
| (filled from CODEOWNERS) | |

## Custom Skills

| Command | Purpose |
|---------|---------|
| `/onboard` | Learn an existing codebase — draft this file from real signals |
| `/setup` | First-time configuration wizard (new or existing) |
| `/plan` | Feature planning with intensity tiers (1=quick, 2=detailed, 3=architectural) |
| `/wrap` | Autonomous session close — review, fix, commit (policy-aware), memory |
| `/check` | Post-build health validation using project commands (read-only) |
| `/guard` | Pre-edit safety check for risky changes |
| `/diff` | Smart diff — groups by module, flags risk level using Risk Surfaces |
| `/undo` | Targeted rollback with safety stash; offers git-revert for committed work |
| `/look` | Visual QA — console errors, state checks, screenshots; service-aware |
| `/save-conversation <slug>` | Save the current session as a verbatim markdown transcript to `Supplementary AI md files/Conversations/` |
